# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env.local
.env*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Python
scripts/__pycache__/

# Personal Claude Code hooks (see .claude/settings.local.json)
.claude/hooks/

# Lavish Editor design-review scratch workspace (large self-contained HTML
# mockups with embedded base64 assets) -- local review tooling output, not
# app source; regenerate via the /lavish skill if a past mockup is needed.
.lavish/

# Local skills-tooling lockfile, not app source.
skills-lock.json
